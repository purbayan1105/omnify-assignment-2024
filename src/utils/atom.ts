import { ObjProps } from "@/components/Modal";
import { atom } from "jotai";

export const collapseAtom = atom(false);
export const modalClick = atom(false);
export const columnAtom = atom([]);
export const toggleMenuAtom = atom(false);
export const filterAtom = atom(false);
